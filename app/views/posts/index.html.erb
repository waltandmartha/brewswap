<p id="notice"><%= notice %></p>
<style>

</style>
<h1>POSTS</h1>

<% unless @posts.nil? %>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Image data</th>
      <th>User</th>
      <th>Profile</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.description %></td>
        <%# <% unless post.image_data.nil? %> 
        <td><%= image_tag post.image_display(:thumb) %></td>
        
        <%# <% end %> 
       
        <td><%= link_to 'VIEW USER PROFILE', profile_path(post.user_id) %> </td>  
        <td><%= link_to 'VIEW POST', post %></td>

            <% end %>
 
      </tr>
    <% end %>
  </tbody>
</table>
<br>

<%= link_to 'NEW POST', new_post_path %>
